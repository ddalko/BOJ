#include <bits/stdc++.h>
using namespace std;

int a, b, f, k;

int main()
{
    scanf("%d %d %d %d",&a,&b,&f,&k);

    int ans = 0;
    if(b < f){
        puts("-1");
        return 0;
    }else{
        int remain = (b - f);

        for(int i = 0; i < k; ++i){
            if(remain)
        }
    }
    printf("%d\n", ans);
}