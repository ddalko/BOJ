#include <cstdio>

int gcd(int a, int b)
{
    return (b == 0) ? a : gcd(b, a%b);
}

int main()
{
    int t;
    int x, y, p, q;
    int u, v;

    for(scanf("%d",&t);t--;){
        scanf("%d %d %d %d",&x,&y,&p,&q);
        if()    
    }
}